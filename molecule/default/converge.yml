---
- name: Converge
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Include workspace"
      ansible.builtin.include_role:
        name: "littlegodzillalaboratory.minecraft_waterland"
